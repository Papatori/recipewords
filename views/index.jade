extends _includes/_layout

block vars
  include _includes/_config
  - var key = "top"
  - var page = pages[key]

block style
  link(rel='stylesheet', href=domain+root+assets_root+"/order/css/main.css")

block contents
  // Contents
  - menu = {};
  //- - var currentMainCategory = "";
  //- - var currentSubCategory = "";

  .columns
    - for (var i = 0; i < menuItems.length; i++){
      //- - var mainCategory = menuItems[i].mainCategory;
      //- - if(mainCategory != currentMainCategory){
      //-   - currentMainCategory = mainCategory;
      //-   h3.title.is-3.mainCategory= currentMainCategory
      //- - }
      //- - var subCategory = menuItems[i].subCategory;
      //- - if(subCategory != currentSubCategory){
      //-   - currentSubCategory = subCategory;
      //-   h5.title.is-5.subCategory= currentSubCategory
      //- - }
      - var menuId = menuItems[i].menuId;
      - menu[menuId] = {};
      - menu[menuId] = menuItems[i];
      - menu[menuId].count = 0;
      - console.log("@views/index menu: " + JSON.stringify(menu))
      //- .card.is-horizontal.columns
      //-   .card-image.column.is-two-fifths
      //-     figure.image.is4by3
      //-       //- img.menu-image(src=menu[menuId].imgUrl, alt=menu[menuId].wordTitle)
      //-       img.menu-image(src=domain+root+assets_root + "/common/img/brand.png", alt=menu[menuId].wordTitle)
      //-   .card-content.column.is-three-fifths
      //-     .content
      //-       p.title.is-5.name= menu[menuId].wordTitle
      //-       p.suptitle.is-6.description= menu[menuId].description

      .card
        .card-content
          .media
            .media-left.media-img
              figure.image.is-48x48
                img.menu-image(src=domain+root+assets_root + "/common/img/spacer.png", alt=menu[menuId].wordTitle)
            .media-content
              p.title.is-5.name= menu[menuId].wordTitle
          .content
            p.suptitle.is-6.description= menu[menuId].description
      - }

block js
    script(type='text/javascript', src=domain+root+assets_root+"/order/js/main.js")
